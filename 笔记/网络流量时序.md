```mermaid
sequenceDiagram
    autonumber
    participant App as 🦠 恶意程序 (用户态)
    participant Lib as 网络库 (ws2_32.dll / schannel)
    participant Kernel as 内核网络栈 (AFD.sys / tcpip.sys)
    participant WFP as 过滤/防火墙层 (WFP / EDR)
    participant Route as 路由决策 & PortProxy
    participant Loopback as 环回适配器 (Npcap Loopback)
    participant NIC as 物理网卡驱动 (NDIS)
    participant Remote as 🌐 远端服务器

    Note over App, Remote: 数据包从产生到发出的完整生命周期 (含抓包分析点)

    App->>+Lib: 1. send() / write() 发送明文数据
    Note over App,Lib: 📍 分析点A (应用层Hook)<br>此处可抓取TLS加密前的明文

    Lib->>Lib: TLS/SSL 加密 (若使用HTTPS)
    Lib->>+Kernel: 2. 系统调用 (NtDeviceIoControlFile)
    Note over Lib,Kernel: 📍 分析点B (内核ETW抓包)<br>执行 netsh trace 可在此捕获

    Kernel->>Kernel: 3. 封装 TCP/UDP 头部 & 校验和
    Kernel->>WFP: 4. 经过过滤层 (进程/端口/IP检查)
    Note over WFP: ⚠️ 拦截点1: 防火墙阻断<br>若在此丢弃，内核直接返回RST

    WFP->>Route: 5. 查询系统路由表 与 转发规则

    alt 场景A: 命中端口转发 (PortProxy / iptables REDIRECT)
        Route->>Route: 修改目标地址为 127.0.0.1
        Route->>+Loopback: 6. 注入环回适配器
        Loopback-->>-App: 数据返回本机监听端口
        Note over Route,Loopback: 🚨 这就是“网卡抓不到包”的原因！<br>📌 分析策略: 抓取环回接口 (lo / Npcap)

    else 场景B: 被主机防火墙/EDR拦截
        Route->>WFP: 命中拦截规则 (Block)
        WFP-->>App: 返回 ICMP不可达 / 丢包
        Note over WFP: 🚨 流量未发出即被扼杀<br>📌 分析策略: 检查 netsh advfirewall 或驱动回调

    else 场景C: 正常外发
        Route->>+NIC: 6. 转发至网卡发送队列
        NIC->>+Remote: 7. 经物理链路发出 (互联网传输)
        Remote-->>-NIC: 8. 返回响应包
        NIC-->>-App: 接收并解包
        Note over NIC,Remote: ✅ 此路径才可被物理网卡抓包<br>📌 分析策略: Wireshark 选物理网卡
    end
```

### 📊 时序图对应的分析策略指引

结合这张图，你之前的疑惑就有了清晰的答案：

1.  **关于“从网卡抓不到”**：
    - 如果流量命中了 **场景A（PortProxy）**，数据包在 `Route` 环节就被拐到了 `Loopback`（环回），根本不会进入 `NIC`（物理网卡）。所以你在 Wireshark 里选物理网卡抓包，自然什么都看不到。
    - **解决办法**：抓包时选择 **Npcap Loopback Adapter**（Windows）或 `lo` 接口（Linux）。

2.  **关于“抓包位置选哪里最优”**：
    - **想解密（看明文）**：选 **分析点A**（用户态）。在程序调用 `send()` 之后，TLS 加密之前，用 Frida 或 API Hook 勾住，直接看原始载荷。
    - **想抓完整网络层数据（看IP/TCP头）**：选 **分析点B**（内核态）。我们之前说的 `netsh trace` 就在这里生效，它能抓到绕过物理网卡的数据包。

3.  **关于“流量异常消失”**：
    - 如果程序既没走环回，也没过网卡，数据直接石沉大海，那就是命中了 **场景B（WFP/EDR拦截）**。这时候流量在内核态就被丢弃了，连 `netsh trace` 都可能看不到发往远端的包（只能看到丢弃日志）。

### 💡 实战操作建议（针对当前任务）

既然你已经运行了 `netsh trace` 抓取了 `.etl` 文件，根据这个时序图：

- 如果恶意程序用了 **场景A**（比如通过 `portproxy` 指向本地），你转换完 `.pcapng` 后，在 Wireshark 里看到的**源IP会是 127.0.0.1**。
- 如果它真的是要发往外网，只是被防火墙卡住了（**场景B**），那你转换出来的文件里可能只有 TCP SYN 包，没有后续的握手或数据载荷。

